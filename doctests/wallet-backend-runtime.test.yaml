name: "Driving the Wallet Backend (4 modules) Against logoscore"
output: wallet-backend-runtime.md
release: ""

intro: |
  `logos-evm-wallet-backend-module` is the **coordinator** for the Logos
  multi-chain EVM wallet. It depends on the eth-rpc, keystore, token-list, and
  uniswap modules and folds in the tx-builder, orchestrating the full send
  pipeline: **build → sign → broadcast → record**.

  This doc-test loads **all six** modules into a `logoscore` daemon and drives
  the coordinator end-to-end against a **local mock JSON-RPC node** (offline,
  reproducible): it configures a chain, imports a known account, and sends a
  native transfer — watching the backend fetch the nonce/fees, build the tx, sign
  it via the keystore, broadcast it via eth-rpc, and record it in local history.

what_you_build: "All five wallet modules, installed with `lgpm` and driven through a `logoscore` daemon: the coordinator orchestrating a real send across its dependencies."

what_you_learn:
  - How a coordinator module declares and calls dependency modules over IPC
  - How the central chain config is pushed down into the eth-rpc module
  - How the send pipeline composes build (alloy) → sign (keystore) → broadcast (eth-rpc) → history

prerequisites:
  - |
    **Nix** with flakes enabled (see [nixos.org](https://nixos.org/download.html)).
  - "**A Linux or macOS machine** with `python3` available (used to run the local mock JSON-RPC node)."

sections:
  - title: "Build logoscore and lgpm"
    step: true
    steps:
      - title: "Build logoscore"
        run: "nix build 'github:logos-co/logos-logoscore-cli#cli' --out-link ./logos"
        check_file: "logos/bin/logoscore"
      - title: "Build lgpm"
        run: "nix build 'github:logos-co/logos-package-manager#cli' -o lgpm"
        check_file: "lgpm/bin/lgpm"

  - title: "Build and install all six modules"
    step: true
    text: |
      The coordinator and its four dependency modules each build to an `.lgx`.
      `logoscore`'s `load-module` does not auto-resolve dependencies, so we install
      all six and load them explicitly below.
    steps:
      - title: "Build the six .lgx packages"
        run: |
          nix build 'github:logos-co/logos-evm-eth-rpc-module#lgx'        --no-write-lock-file -o eth-rpc-lgx
          nix build 'github:logos-co/logos-evm-keystore-module#lgx'       --no-write-lock-file -o keystore-lgx
          nix build 'github:logos-co/logos-evm-token-list-module#lgx'     --no-write-lock-file -o token-list-lgx
          nix build 'github:logos-co/logos-evm-uniswap-module#lgx'        --no-write-lock-file -o uniswap-lgx
          nix build 'github:logos-co/logos-evm-fee-module#lgx'          --no-write-lock-file -o fee-lgx
          nix build 'github:logos-co/logos-evm-wallet-backend-module{release}#lgx' --no-write-lock-file -o backend-lgx
        code_block: |
          nix build 'github:logos-co/logos-evm-eth-rpc-module#lgx'        -o eth-rpc-lgx
          nix build 'github:logos-co/logos-evm-keystore-module#lgx'       -o keystore-lgx
          nix build 'github:logos-co/logos-evm-token-list-module#lgx'     -o token-list-lgx
          nix build 'github:logos-co/logos-evm-uniswap-module#lgx'        -o uniswap-lgx
          nix build 'github:logos-co/logos-evm-fee-module#lgx'          -o fee-lgx
          nix build 'github:logos-co/logos-evm-wallet-backend-module{release}#lgx' -o backend-lgx
      - title: "Seed the capability module"
        run: |
          mkdir -p modules
          cp -RL ./logos/modules/. ./modules/
          # `cp` without -p keeps the Nix store's own 0555 directory mode, and a
          # read-only DIRECTORY refuses every new file and every unlink (POSIX
          # consults the parent's write bit, not the file's). That wedges any
          # later install into this tree, and makes `rm -rf` in a teardown or a
          # re-run fail with Permission denied.
          chmod -R u+w ./modules
        check_file: "modules/capability_module/manifest.json"
      - title: "Install all six .lgx"
        run: |
          for p in eth-rpc-lgx keystore-lgx token-list-lgx uniswap-lgx fee-lgx backend-lgx; do
            ./lgpm/bin/lgpm --modules-dir ./modules --allow-unsigned install --file "$p"/*.lgx
          done
        expect_contains:
          - "Installed to:"
      - title: "Confirm the installs"
        run: "./lgpm/bin/lgpm --modules-dir ./modules list"
        expect_contains:
          - "wallet_backend_module"
          - "eth_rpc_module"
          - "keystore_module"
          - "token_list_module"
          - "uniswap_module"
          - "fee_module"

  - title: "Start a mock JSON-RPC node"
    step: true
    steps:
      - title: "Write the mock node"
        text: "Answers the methods the send pipeline needs (chainId 1337) with canned values."
        file:
          path: mock_node.py
          content: |
            import http.server, json
            RES = {
                "eth_chainId": "0x539",                 # 1337
                "eth_getTransactionCount": "0x0",
                "eth_gasPrice": "0x3b9aca00",
                "eth_estimateGas": "0x5208",
                "eth_sendRawTransaction": "0x" + "ab" * 32,
            }
            class H(http.server.BaseHTTPRequestHandler):
                def do_POST(self):
                    n = int(self.headers.get('content-length', 0))
                    req = json.loads(self.rfile.read(n) or b'{}')
                    body = json.dumps({"jsonrpc": "2.0", "id": req.get("id", 1),
                                       "result": RES.get(req.get("method"), "0x0")}).encode()
                    self.send_response(200)
                    self.send_header('content-length', str(len(body)))
                    self.end_headers()
                    self.wfile.write(body)
                def log_message(self, *a): pass
            http.server.HTTPServer(('127.0.0.1', 8602), H).serve_forever()
      - title: "Start the mock node"
        run: "sh -c 'python3 mock_node.py > mock.log 2>&1 &'"
        code_block: "python3 mock_node.py &"
      - run: "sleep 2"

  - title: "Run the daemon and drive the send pipeline"
    step: true
    steps:
      - title: "Write the inputs"
        file:
          path: chains.json
          content: |
            [ { "chainId": 1337, "name": "Mock", "rpcUrl": "http://127.0.0.1:8602", "nativeSymbol": "ETH" } ]
      - title: "Write the account import (Foundry's known test mnemonic)"
        file:
          path: mnemonic.json
          content: |
            { "phrase": "test test test test test test test test test test test junk", "accountIndex": 0, "password": "pw" }
      - title: "Write the send request"
        file:
          path: send.json
          content: |
            {
              "from": "0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266",
              "to": "0x70997970C51812dc3A010C7d01b50e0d17dc79C8",
              "chainId": 1337,
              "amount": "0xde0b6b3a7640000"
            }
      - title: "Start the daemon"
        run: "sh -c './logos/bin/logoscore -D -m ./modules > logs.txt 2>&1 &'"
        code_block: "logoscore -D -m ./modules > logs.txt &"
      - run: "sleep 3"
      - title: "Load all six modules"
        run: |
          for m in eth_rpc_module keystore_module token_list_module uniswap_module fee_module wallet_backend_module; do
            ./logos/bin/logoscore load-module "$m"
          done
        code_block: |
          for m in eth_rpc_module keystore_module token_list_module uniswap_module fee_module wallet_backend_module; do
            logoscore load-module "$m"
          done
        expect_contains:
          - "wallet_backend_module"
      - title: "Configure the chain (pushed down into eth-rpc)"
        # set_chains pushes config into eth-rpc (cross-module) and is idempotent,
        # so retry it on a transient RPC_FAILED too.
        run: |
          for i in 1 2 3 4 5 6; do
            out=$(./logos/bin/logoscore call wallet_backend_module set_chains @chains.json 2>&1)
            case "$out" in *RPC_FAILED*) sleep 3 ;; *) break ;; esac
          done
          printf '%s\n' "$out"
        code_block: "logoscore call wallet_backend_module set_chains @chains.json"
        expect_contains:
          - "true"
      - title: "Test the endpoint (backend → eth-rpc → mock node)"
        # Retry on a transient RPC_FAILED — this is the first call that drives the
        # backend through a cross-module network round-trip (→ eth-rpc → mock),
        # which can hit a transport race on loaded CI runners. test_endpoint is a
        # pure read, so retrying is safe (and it warms the path for later steps).
        run: |
          for i in 1 2 3 4 5 6; do
            out=$(./logos/bin/logoscore call wallet_backend_module test_endpoint 1337 2>&1)
            case "$out" in *RPC_FAILED*) sleep 3 ;; *) break ;; esac
          done
          printf '%s\n' "$out"
        code_block: "logoscore call wallet_backend_module test_endpoint 1337"
        expect_contains:
          - "chainId"
      - title: "Name the custodian before importing (keystore Tier D)"
        text: |
          Importing a key is Tier D: it belongs to the CUSTODIAN alone, and the built-in
          default is `keystore_ui`, which this daemon does not run — so the import below
          would be refused until someone says who holds the role.

          `configure` is TOTAL: a role it does not name is held by NOBODY, not left at its
          default. So this names both, and `approver` deliberately stays `signer_ui` —
          no approver runs here, which is exactly what the approval steps prove.
        file:
          path: roles.json
          content: |
            { "approver": "signer_ui", "custodian": "wallet_backend_module" }
        run: "./logos/bin/logoscore call keystore_module configure @roles.json"
        code_block: "logoscore call keystore_module configure @roles.json"
        expect_contains:
          - "wallet_backend_module"
      - title: "Import a known account (backend → keystore)"
        run: "./logos/bin/logoscore call wallet_backend_module import_mnemonic @mnemonic.json main"
        code_block: "logoscore call wallet_backend_module import_mnemonic @mnemonic.json main"
        expect_contains:
          - "f39Fd6e51aad88F6F4ce6aB8827279cffFb92266"
      - title: "List accounts"
        run: |
          for i in 1 2 3 4 5 6; do
            out=$(./logos/bin/logoscore call wallet_backend_module list_accounts 2>&1)
            case "$out" in *RPC_FAILED*) sleep 3 ;; *) break ;; esac
          done
          printf '%s\n' "$out"
        expect_contains:
          - "f39Fd6e51aad88F6F4ce6aB8827279cffFb92266"
      - title: "There is no unlock, and no way to sign"
        text: |
          The wallet used to unlock an account and then sign with it for as long
          as the process lived. Both `unlock` and `lock` are gone, and the
          keystore has no `sign_*` for this module to call either. The daemon
          lists what a module exposes when asked for a method it does not have,
          so the removal can be asserted directly rather than implied.
        run: |
          out=$(./logos/bin/logoscore call wallet_backend_module unlock 2>&1 || true)
          printf '%s\n' "$out"
          case "$out" in
            *'"unlock"'*) echo "GONE=no  (unlock is still exposed)" ;;
            *)            echo "GONE=yes (no unlock in available_methods)" ;;
          esac
        code_block: "logoscore call wallet_backend_module unlock <address> pw   # no such method"
        expect_contains:
          - "GONE=yes"
      - title: "A send is now a REQUEST, and returns no hash"
        text: |
          The coordinator still fetches the nonce and gas price from eth-rpc,
          estimates gas and builds the unsigned EIP-1559 tx (alloy) — then stops.
          It calls `request_approval`, parks the job and answers immediately with
          a request id. There is no hash to return: nothing has been signed, so
          nothing has been broadcast.
        run: |
          # The daemon returns the module's JSON as an ESCAPED string inside its
          # own envelope ("result":"{\"ok\":true,...}"), so unescape once here
          # and assert on clean JSON. Matching the raw form is how a guard ends
          # up vacuous: `case $out in *'"hash":"0x'*)` can never fire against
          # \"hash\":\"0x, so it would report "not signed" even if it were.
          out=$(./logos/bin/logoscore call wallet_backend_module send_native @send.json 2>&1 | sed 's/\\"/"/g')
          printf '%s\n' "$out"
          case "$out" in
            *abababab*) echo "SIGNED_WITHOUT_APPROVAL=yes" ;;
            *)          echo "SIGNED_WITHOUT_APPROVAL=no" ;;
          esac
        code_block: "logoscore call wallet_backend_module send_native @send.json"
        expect_contains:
          - '"pending":true'
          - "requestId"
          - "SIGNED_WITHOUT_APPROVAL=no"
      - title: "It is waiting for a human, not for the chain"
        text: |
          `send_status` reports where the request actually is. No approver runs
          in this daemon — Tier A admits `module:signer_ui` and nothing else, and
          a `logoscore` caller is refused there by design — so the request stays
          parked. That is the point: the wallet cannot advance it alone.
        run: |
          out=$(./logos/bin/logoscore call wallet_backend_module send_native @send.json 2>&1 | sed 's/\\"/"/g')
          rid=$(printf '%s' "$out" | sed -n 's/.*"requestId":"\([^"]*\)".*/\1/p')
          printf 'requestId=%s\n' "$rid"
          ./logos/bin/logoscore call wallet_backend_module send_status "$rid" 2>&1 | sed 's/\\"/"/g' || true
        code_block: "logoscore call wallet_backend_module send_status <requestId>"
        expect_contains:
          - "awaiting_approval"
      - title: "Nothing was broadcast, so history has no hash"
        text: |
          History is written when a transaction is BROADCAST, not when it is
          requested. Recording at request time would show the user a transaction
          they had not approved, carrying a hash that does not exist.

          The full approved path — request, render, vault password, signature —
          is proven end to end in `logos-evm-signer-ui`'s own doc-test, which
          runs an approver. This spec deliberately proves the other half: that
          without one, nothing is signed.
        run: |
          for i in 1 2 3 4 5 6; do
            out=$(./logos/bin/logoscore call wallet_backend_module get_history f39fd6e51aad88f6f4ce6ab8827279cfffb92266 2>&1 | sed 's/\\"/"/g')
            case "$out" in *RPC_FAILED*) sleep 3 ;; *) break ;; esac
          done
          printf '%s\n' "$out"
          case "$out" in
            *abababab*) echo "BROADCAST_WITHOUT_APPROVAL=yes" ;;
            *)          echo "BROADCAST_WITHOUT_APPROVAL=no" ;;
          esac
        code_block: "logoscore call wallet_backend_module get_history <address>"
        expect_contains:
          - "BROADCAST_WITHOUT_APPROVAL=no"
      - title: "Stop the daemon and the mock node"
        run: |
          trap '' TERM
          ./logos/bin/logoscore stop || true
          pkill -f mock_node.py 2>/dev/null || true
          true
      - run: "sleep 2"
      - title: "Confirm the daemon has stopped"
        run: "./logos/bin/logoscore status || true"
        expect_contains:
          - '"status":"not_running"'
